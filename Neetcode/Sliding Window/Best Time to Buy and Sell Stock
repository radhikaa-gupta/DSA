class Solution {
    public int maxProfit(int[] prices) {
        int max = 0;
        int buy = prices[0];
        for (int price : prices){
            max = Math.max(max, price - buy);
            buy = Math.min(buy, price);
        }
        return max;
    }
}
