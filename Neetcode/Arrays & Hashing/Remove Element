class Solution {
    public int removeElement(int[] nums, int val) {
        
        // int decrement = 0;
        // int i = 0;
        // while(i < nums.length){
        //     while (i < nums.length && nums[i] == val) {decrement++; i++;};
        //     if(i < nums.length && !(i - decrement < 0)) nums[i - decrement] = nums[i]; 
        //     i++;
        // }
        // return nums.length - decrement;

        // 2 pointer
        int i = 0; int j = 0;
        while(j < nums.length){
            nums[i] = nums[j];
            if (!(nums[i] == val)){
               i++;
            }
            j++;
        }
        return i;

    }
}
